---
// filepath: /F:/Repos/codedesignplus.github.io/src/pages/404.astro
import Layout from '../layouts/Layout.astro';
import Container from '../components/Container.astro';
---

<Layout title="Página no encontrada - CodeDesignPlus">
    <Container>
        <div class="flex items-center justify-center min-h-screen py-16 lg:py-20">
            <div class="max-w-4xl mx-auto text-center animate-slide-in">
                <h1 class="text-9xl font-bold text-gray-900 dark:text-white mb-8">
                    404
                </h1>
                <p class="text-4xl font-semibold text-gray-900 dark:text-white mb-8">
                    Página no encontrada
                </p>
                <p class="text-gray-600 dark:text-gray-300 mb-6">
                    Lo sentimos, la página que estás buscando no existe.
                </p>
                <a
                    href="/"
                    class="inline-block bg-primary text-white py-2 px-4 rounded-md hover:bg-primary/90 transition-colors"
                >
                    Volver al Inicio
                </a>
            </div>
        </div>
    </Container>
</Layout>

<style>
    @keyframes slideIn {
        from {
            opacity: 0;
            transform: translateY(100%);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .animate-slide-in {
        animation: slideIn 2s ease-in-out forwards;
    }
</style>