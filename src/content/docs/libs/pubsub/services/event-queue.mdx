---
title: IEventQueueService
---

`IEventQueueService` es una interfaz que define los métodos necesarios para gestionar una cola de eventos de dominio. Esta interfaz permite agregar eventos a la cola y procesarlos de manera asíncrona.

```csharp url="packages/CodeDesignPlus.Net.PubSub/src/CodeDesignPlus.Net.PubSub.Abstractions/IEventQueueService.cs"
```

## Métodos
---

Los métodos que se pueden utilizar con la interfaz `IEventQueueService` son los siguientes:

### EnqueueAsync
**Type:** `Task EnqueueAsync(IDomainEvent @event, CancellationToken cancellationToken)`

Agrega un evento al final de la cola concurrente.

### DequeueAsync
**Type:** `Task DequeueAsync(CancellationToken token)`

Intenta eliminar y devolver el evento al principio de la cola concurrente.

## Implementación
---

La implementación de la interfaz `IEventQueueService` se muestra a continuación. Esta implementación es responsable de almacenar los eventos en una cola en memoria antes de ser procesados.

```csharp url="packages/CodeDesignPlus.Net.PubSub/src/CodeDesignPlus.Net.PubSub/Services/EventQueueService.cs"
```

## Ejemplo de Uso
---

Para comprender el funcionamiento y el uso de la interfaz `IEventQueueService`, se muestra la clase `PubSubService` que implementa la interfaz `IPubSub` y utiliza el servicio `IEventQueueService` para encolar eventos de dominio cuando `UseQueue` es `true`.

```csharp url="packages/CodeDesignPlus.Net.PubSub/src/CodeDesignPlus.Net.PubSub/Services/PubSubService.cs"
```